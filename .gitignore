# Environments / caches
venv/
__pycache__/
*.pyc
.DS_Store
.env

# Archived / non-production material — kept locally, not versioned
archive/

# Results: version ONLY the SISPAD paper results dir, ignore everything else
results/*
!results/sispad_scba_2026-04-14/

# papers/ (reference PDFs + LaTeX sources + poster) is now versioned.
